import { DataType } from 'omp-schema';

export interface DataTypesEditorProps {
    currentItem: DataType;
}

const DataTypesEditor = (props: DataTypesEditorProps) => (
    null
);

export default DataTypesEditor;
